{
  "name": "@devspace/vscode-apidoc",
  "version": "0.1.0",
  "description": "",
  "main": "lib/index",
  "types": "lib/index",
  "scripts": {
    "build": "npm run clean && npm run compile",
    "clean": "rimraf -rf ./lib",
    "compile": "tsc -p tsconfig.build.json",
    "prepublishOnly": "npm run build",
    "test": "npm run build"
  },
  "author": "rayl",
  "dependencies": {
    "@devspace/vscode-core": "^0.1.0"
  }
}
